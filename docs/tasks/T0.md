# T0 — Scaffold

## 근거
- 볼트 `60 Production Pipeline Plan.md` §5 (M0 체크리스트)

## 산출물
- Unity 6000.3.19f1 프로젝트 (`-createProject`, 배치모드)
- `.gitignore` · `.gitattributes`
- `scripts/launch.ps1` · `scripts/gate.ps1`
- asmdef 4종: `Core`(noEngineReferences) / `Runtime` / `Editor` / `Tests.EditMode`
- `CocClear.Core.DeterministicRandom` + FNV-1a `Hash`
- EditMode 결정성 테스트 5건
- `docs/` (CONVENTIONS · licenses · tasks · design)
- Sdp 데이터 레인 비계: `tools/DataSchema/Records/GameRecords.cs`, `tools/data/README.md`, `Data/Source|Generated`

## 완료 정의
- [x] `scripts/gate.ps1` → GATE PASS
- [x] `origin/main`에 push 확인 (`git rev-parse origin/main`)

## Non-scope
- 게임 도메인 코드 없음. `ItemRecord`는 파이프라인을 한 번 관통시키기 위한 **자리표시자**이며, 도메인 확정 시 교체한다.
- 렌더 파이프라인(URP 등) 미도입 — 아트 디렉션 미결(볼트 원장 A3).
- 씬 없음. 빌드 씬은 도메인 확정 후.

## 함정 (적용분)
- A1: 배치 env에 `ProgramData` 누락 시 UPM IPC 실패 → `error CS` 0건이라 원인 오인 → `gate.ps1`/`launch.ps1`이 env parity 보장
- A2: `-quit` + `-runTests` 병용 금지 → `gate.ps1`에서 `-quit` 미사용
- E1: env 인라인 금지, `.ps1`로
